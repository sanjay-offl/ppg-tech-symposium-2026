<section class="about" id="about"><div class="container about-grid"><div><span class="section-label">About the Event</span><h2>SHOW YOUR<br />TALENT,<br />EARN YOUR<br />GLORY.</h2><p>VIYUGAM 2K26 is the premier national-level technical symposium at PPG Institute of Technology, Coimbatore - a full-day celebration of engineering, innovation, and creativity spanning 8 departments with 40+ competitive events, cash prizes, and a community of the best minds in the country.</p><div class="badges"><span>NAAC A Grade</span><span>AICTE Approved</span><span>Anna University</span><span>ISO 9001:2015</span></div></div><div class="perks"><article><b>🏆</b><h3>Exciting Cash Prizes</h3><p>Winners across all events take home exciting cash rewards.</p></article><article><b>📜</b><h3>Certificates</h3><p>Participation certificates provided to all attendees.</p></article><article><b>🍱</b><h3>Lunch Included</h3><p>Lunch and refreshments for all registered participants.</p></article><article><b>🎯</b><h3>40+ Events</h3><p>Technical and non-technical events across 8 disciplines.</p></article></div></div></section>
<style lang="scss">
@use '../../styles/variables' as *;

.about {
  position: relative;
  overflow: hidden;
  padding: 105px 0;
  background-color: #f8fafc;
  color: $black;

  /* Prominent shaded grid background effect */
  background-image: 
    /* Soft vignette shading & center spotlight */
    radial-gradient(ellipse at 50% 50%, rgba(255, 255, 255, 0.92) 0%, rgba(241, 245, 249, 0.88) 60%, rgba(226, 232, 240, 0.96) 100%),
    /* Ambient warm orange glow top-left */
    radial-gradient(circle at 15% 20%, rgba(255, 101, 0, 0.12) 0%, transparent 45%),
    /* Ambient accent glow bottom-right */
    radial-gradient(circle at 85% 80%, rgba(255, 122, 0, 0.08) 0%, transparent 50%),
    /* Major grid lines (120px) */
    linear-gradient(rgba(15, 23, 42, 0.08) 1px, transparent 1px),
    linear-gradient(90deg, rgba(15, 23, 42, 0.08) 1px, transparent 1px),
    /* Minor grid lines (24px) */
    linear-gradient(rgba(15, 23, 42, 0.035) 1px, transparent 1px),
    linear-gradient(90deg, rgba(15, 23, 42, 0.035) 1px, transparent 1px);
  background-size: 
    100% 100%, 
    100% 100%, 
    100% 100%, 
    120px 120px, 
    120px 120px, 
    24px 24px, 
    24px 24px;
  background-position: 
    0 0, 
    0 0, 
    0 0, 
    -1px -1px, 
    -1px -1px, 
    -1px -1px, 
    -1px -1px;

  border-top: 1px solid rgba(255, 101, 0, 0.18);
  border-bottom: 1px solid rgba(15, 23, 42, 0.08);

  /* Top glowing orange line */
  &::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 65%;
    height: 2px;
    background: linear-gradient(90deg, transparent, $orange, transparent);
    opacity: 0.9;
  }
}

.about-grid {
  position: relative;
  z-index: 2;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: clamp(40px, 8vw, 110px);
}

.about h2 {
  margin: 18px 0;
  font: clamp(3rem, 5vw, 4.5rem)/.88 $display;
  color: $black;
  letter-spacing: 0.01em;
}

.about p {
  max-width: 480px;
  color: #334155;
  line-height: 1.75;
  font-size: 0.98rem;
}

.badges {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 32px;
}

.badges span {
  display: inline-flex;
  align-items: center;
  padding: 8px 16px;
  border: 1px solid rgba(15, 23, 42, 0.1);
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  color: #0f172a;
  font-size: .74rem;
  font-weight: 600;
  letter-spacing: 0.02em;
  box-shadow: 0 4px 14px rgba(15, 23, 42, 0.04);
  transition: all 0.25s ease;

  &:hover {
    background: #ffffff;
    border-color: rgba(255, 101, 0, 0.4);
    color: $orange;
    transform: translateY(-2px);
    box-shadow: 0 6px 18px rgba(255, 101, 0, 0.15);
  }
}

.badges span::before {
  content: '';
  display: inline-block;
  width: 6px;
  height: 6px;
  margin-right: 8px;
  border-radius: 50%;
  background: $orange;
  box-shadow: 0 0 6px rgba(255, 101, 0, 0.6);
}

.perks {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
}

.perks article {
  position: relative;
  padding: 24px;
  border: 1px solid rgba(15, 23, 42, 0.08);
  border-radius: 16px;
  background: rgba(255, 255, 255, 0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  box-shadow: 0 10px 28px rgba(15, 23, 42, 0.04), 0 1px 3px rgba(0, 0, 0, 0.02);
  transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1);

  &::before {
    content: '';
    position: absolute;
    top: 0;
    left: 16px;
    right: 16px;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255, 101, 0, 0.25), transparent);
    opacity: 0;
    transition: opacity 0.35s ease;
  }

  &:hover {
    transform: translateY(-5px);
    border-color: rgba(255, 101, 0, 0.4);
    background: linear-gradient(135deg, #ffffff 0%, rgba(255, 246, 238, 0.85) 100%);
    box-shadow: 0 18px 40px rgba(255, 101, 0, 0.14), 0 4px 12px rgba(15, 23, 42, 0.03);

    &::before {
      opacity: 1;
    }

    b {
      transform: scale(1.15);
    }
  }
}

.perks b {
  display: inline-block;
  font-size: 1.6rem;
  transition: transform 0.3s ease;
}

.perks h3 {
  margin: 14px 0 6px;
  font-size: 1rem;
  font-weight: 700;
  color: $black;
  letter-spacing: 0.01em;
}

.perks p {
  margin: 0;
  color: #475569;
  font-size: .8rem;
  line-height: 1.55;
}

@media (max-width: 760px) {
  .about { padding: 75px 0; }
  .about-grid { grid-template-columns: 1fr; gap: 40px; }
  .perks { grid-template-columns: 1fr 1fr; }
}

@media (max-width: 420px) {
  .perks { grid-template-columns: 1fr; }
}
</style>

<nav class="navbar" aria-label="Primary navigation">
  <a class="brand" href="/" aria-label="VIYUGAM 2K26 home"><img class="brand-logo" src="/images/departments/logo.avif" alt="PPG Institute of Technology logo" /><span>VIYUGAM <b>2K26</b></span></a>
  <button class="menu-toggle" type="button" aria-expanded="false" aria-controls="primary-links"><span class="sr-only">Toggle navigation</span><span></span><span></span></button>
  <ul class="nav-links" id="primary-links">
    <li><a href="/about">About</a></li><li><a href="/events">Events</a></li><li><a href="/departments">Departments</a></li><li><a href="/schedule">Schedule</a></li><li><a class="nav-cta" href="/register">Register · ₹250</a></li>
  </ul>
</nav>
<style lang="scss">
@use '../../styles/variables' as *;
.navbar { position: fixed; z-index: 10; top: 20px; left: 50%; transform: translateX(-50%); width: min(920px, calc(100% - 32px)); min-height: 54px; display: flex; align-items: center; justify-content: space-between; padding: 8px 12px; border: 1px solid rgba(255,255,255,.9); border-radius: 999px; background: rgba(255,255,255,.8); backdrop-filter: blur(24px); box-shadow: 0 4px 24px rgba(0,0,0,.08); transition: background .25s, box-shadow .25s; }
.brand { display: inline-flex; align-items: center; gap: 8px; font: 700 14px/1 $technical; text-decoration: none; letter-spacing: .04em; white-space: nowrap; }.brand-logo { display: block; width: 32px; height: 32px; object-fit: contain; flex: 0 0 32px; }.brand b { color: $orange; }.nav-links { display: flex; align-items: center; gap: 2px; margin: 0; padding: 0; list-style: none; }.nav-links a { display: inline-flex; align-items: center; min-height: 36px; padding: 0 12px; border-radius: 999px; font-size: 12.5px; line-height: 1; text-decoration: none; }.nav-links a:hover { background: rgba(0,0,0,.06); }.nav-links .nav-cta { color: white; background: $orange; font-weight: 700; box-shadow: 0 3px 14px $orange-glow; }.menu-toggle { display: none; border: 0; background: transparent; }
@media (max-width: 760px) { .navbar { top: 12px; min-height: 50px; padding: 7px 10px; }.brand { font-size: 13px; }.brand-logo { width: 30px; height: 30px; flex-basis: 30px; }.menu-toggle { display: grid; gap: 4px; padding: 8px; cursor: pointer; }.menu-toggle span:not(.sr-only) { width: 22px; height: 2px; background: $black; }.nav-links { position: absolute; top: calc(100% + 8px); left: 0; right: 0; display: none; padding: 8px; border-radius: 18px; background: white; box-shadow: 0 12px 28px rgba(0,0,0,.12); }.nav-links.is-open { display: block; }.nav-links a { min-height: 42px; padding: 0 14px; }.nav-links .nav-cta { justify-content: center; margin-top: 4px; } }
</style>
<script>
const toggle = document.querySelector<HTMLButtonElement>('.menu-toggle');
const links = document.querySelector<HTMLElement>('.nav-links');
toggle?.addEventListener('click', () => { const open = links?.classList.toggle('is-open') ?? false; toggle.setAttribute('aria-expanded', String(open)); });
window.addEventListener('scroll', () => document.querySelector('.navbar')?.classList.toggle('is-scrolled', window.scrollY > 60), { passive: true });
</script>
